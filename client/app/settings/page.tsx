"use client";
import { ProtectedWrapper } from "@/utils/ProtectedWrapper";

export default function Settings() {
  return (
    <ProtectedWrapper>
      <main className="z-0 min-h-screen min-w-full p-10">settings...</main>
    </ProtectedWrapper>
  );
}
